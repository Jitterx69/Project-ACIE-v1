# TEMPLATE - Replace values before applying
# Store actual secrets in Kubernetes Secrets or use a secrets manager
apiVersion: v1
kind: Secret
metadata:
  name: acie-secrets
  namespace: acie
type: Opaque
stringData:
  # Redis (if password enabled)
  redis-password: "REPLACE_WITH_SECURE_PASSWORD"

  # Database
  postgres-password: "REPLACE_WITH_SECURE_PASSWORD"
  postgres-user: "acie"

  # API Keys (if using authentication)
  api-key: "REPLACE_WITH_API_KEY"
  jwt-secret: "REPLACE_WITH_JWT_SECRET"
# Usage in deployment:
# env:
# - name: REDIS_PASSWORD
#   valueFrom:
#     secretKeyRef:
#       name: acie-secrets
#       key: redis-password
